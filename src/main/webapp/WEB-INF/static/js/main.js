function edit(id, description, status) {
    document.getElementById("taskId").value = id;
    document.getElementById("description").value = description;
    document.getElementById("status").value = status;
}